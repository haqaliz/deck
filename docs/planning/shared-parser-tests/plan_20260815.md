# Plan: shared-parser-tests

Date: 2026-08-15. Input: `docs/planning/shared-parser-tests/prd.md` (approved).
Branch: `feat/shared-parser-tests/aliz` (worktree `.claude/worktrees/feat-shared-parser-tests`).
All work happens in the worktree. One commit per phase.

Test command throughout: `xcodegen generate --spec native/project.yml && xcodebuild test
-project native/Deck.xcodeproj -scheme DeckSharedTests -derivedDataPath native/build`.

---

## P1 — Test target + scheme + harness smoke test

**Goal:** `DeckSharedTests` exists, runs via `xcodebuild test`, and the runner
proves it executes tests (guards the "0 tests passes silently" gap).

**Files touched:** `native/project.yml`, `native/SharedTests/SmokeTests.swift` (new).

**Steps:**
1. Add `DeckSharedTests` target to project.yml: `type: bundle.unit-test`,
   sources `[Shared, SharedTests]`, `SWIFT_VERSION 5.10`, macOS 15.0,
   `GENERATE_INFOPLIST_FILE: YES`, `OTHER_LDFLAGS: $(inherited) -lsqlite3`,
   automatic signing / team K6X49DG8VF (mirror DeckAgent settings).
2. Add explicit `schemes:` section: `DeckSharedTests` scheme with the test
   action on the new target (no build action needed beyond test).
3. Add `native/SharedTests/SmokeTests.swift` — one assertion against
   `GitBoxSnapshot.dayCounts(from:)` with a tiny `git log --pretty`-shaped
   fixture (proves Shared compiles into the bundle).
4. Run the test command; confirm output shows the smoke test executing
   (e.g. "Executed N tests").

**Verification:** test command green, output reports ≥1 test executed.
**Dependencies:** none.

## P2 — OpenBox pure-logic extraction to Shared

**Goal:** `ModelParser`, `CostSeries`, `OpenCodeFormatters` become internal
code in `native/Shared/OpenBoxCore.swift`; OpenBoxWidget.swift drops its
private copies (PRD §4.1).

**Files touched:** `native/Shared/OpenBoxCore.swift` (new), `native/DeckWidgets/OpenBoxWidget.swift`.

**Steps:**
1. Copy the three private enums verbatim into `Shared/OpenBoxCore.swift`
   (internal; `CostSeries.Series/Point` need `Identifiable` for `Point` — keep
   the UUID id; it compiles without WidgetKit).
2. Delete the private copies from OpenBoxWidget.swift (lines 444-602).
3. Build DeckApp (Debug) — call sites are unchanged, so this is a pure move.

**Verification:** `xcodebuild build -scheme DeckApp` green; `xcodebuild test
-scheme DeckSharedTests` still green.
**Dependencies:** P1 (test target compiles Shared).

## P3 — OpenCodeReader SQL test hook

**Goal:** SQL strings + a db-handle entry point are internal so the SQL suite
(P8) can run them against a fixture DB (PRD §4.2).

**Files touched:** `native/Shared/OpenCodeReader.swift`.

**Steps:**
1. Change the five SQL constants from `private static let` to `static let`
   (internal).
2. Add `static func load(from db: OpaquePointer) -> OpenCodeSnapshot?` holding
   today's body of `load()`; `load()` keeps `dbPath` open + `busy_timeout` and
   delegates to it.
3. Keep `rows(_:_:)` private for now (P8 raises it only if the SQL suite needs
   raw rows; prefer asserting on `load(from:)` output).

**Verification:** `xcodebuild build -scheme DeckApp` green; test command green.
**Dependencies:** P1.

## P4 — Ported suites: settings, tools, cost, model/formatters

**Goal:** permanent tests for `DeckSettings` tolerant decode, `mapToolRows`,
`CostSeries`, `ModelParser`, `OpenCodeFormatters`.

**Files touched:** `native/SharedTests/` — `DecodeTests.swift`, `ToolCountTests.swift`,
`CostSeriesTests.swift`, `ModelParserTests.swift` (new).

**Steps:**
1. `DecodeTests.swift` — re-express 8ca082b (131 ln) against the current
   `DeckSettings` structs (corrupted JSON per struct → tolerant defaults).
2. `ToolCountTests.swift` — re-express 2c899e7 (82 ln) against
   `OpenCodeReader.mapToolRows`.
3. `CostSeriesTests.swift` — re-express e8e1c11 (179 ln) against the moved
   `CostSeries` (top-N ordering, tie order, "other" merge, points flattening,
   displayID).
4. `ModelParserTests.swift` — fresh: JSON-object model strings, `provider/id-variant`,
   variant suffix stripping, embedded-variant fallback, colon form.
   Plus `OpenCodeFormatters` cases (K/M/B token grouping, cost format).

**Verification:** test command green.
**Dependencies:** P2 (moved enums), P1.

## P5 — Ported suites: ShipBox + ClipBox

**Goal:** permanent tests for `ShipBoxSnapshot` mapping/parse/format and
`ClipBoxSnapshot` classifier/previews/merge.

**Files touched:** `native/SharedTests/ShipBoxSnapshotTests.swift`,
`native/SharedTests/ClipBoxSnapshotTests.swift` (new).

**Steps:**
1. ShipBox — re-express f0c94c4 (197 ln) against the merged
   `ShipBoxSnapshot.map(status:conclusion:)` + parse/format helpers; include
   GitHub JSON fixture snippets for run-status parsing.
2. ClipBox — re-express d2f81a0 (139 ln) against `ClipBoxSnapshot.kind(for:)`,
   `make`, `dedupeKey`, `merge`, `sizeString` (pasteboard-typed fixtures, no
   live NSPasteboard).

**Verification:** test command green.
**Dependencies:** P1.

## P6 — Ported suite: HomeBox

**Goal:** permanent tests for `HomeBoxSnapshot` wttr parsing, icon mapping,
zone rows.

**Files touched:** `native/SharedTests/HomeBoxSnapshotTests.swift`,
`native/SharedTests/Fixtures/amsterdam_j1.json` (new).

**Steps:**
1. Restore the amsterdam fixture from 989999a (1366 ln) into
   `SharedTests/Fixtures/`.
2. Re-express WttrParserTests/WeatherIconTests/ZoneRowsTests (150 ln) against
   the merged `HomeBoxSnapshot.parse(_:)`, `symbol(for:)`, `build(identifiers:at:)`
   (pass explicit dates for zone rows).

**Verification:** test command green.
**Dependencies:** P1.

## P7 — Fresh suite: GitBox

**Goal:** tests for `GitBoxSnapshot` parser/math/formatters (never had scratch
tests — written fresh from behavior).

**Files touched:** `native/SharedTests/GitBoxSnapshotTests.swift` (new).

**Steps:**
1. `dayCounts(from:)` — `git log --pretty`-shaped fixture lines, day labels,
   multi-day counts, empty input.
2. `daysBack`/`bucket` — window alignment with injected today.
3. `streak` — grace rule (empty today → yesterday anchor; today counts;
   no commits → 0).
4. `shortName` + `GitBoxFormatters` — path collapsing, count grouping.

**Verification:** test command green.
**Dependencies:** P1 (SmokeTests already proves Shared linkage).

## P8 — DB SQL suite (fixture schema)

**Goal:** the five real SQL queries execute against an in-memory fixture DB
with the opencode schema and map into `OpenCodeSnapshot` correctly.

**Files touched:** `native/SharedTests/OpenCodeSQLTests.swift` (new), possibly
`native/Shared/OpenCodeReader.swift` (raise `rows(_:_:)` to internal only if
the suite needs raw-row asserts).

**Steps:**
1. Open an in-memory DB (`sqlite3_open(":memory:")`), create `session` and
   `part` tables matching the SQL strings' columns (`time_created` ms epoch,
   `tokens_input`, `tokens_output`, `cost`, `model`, `data` JSON text).
2. Insert rows with **relative** timestamps (`strftime('%s','now')*1000 - N*86400000`)
   so today/window queries are stable.
3. Assert `OpenCodeReader.load(from:)` fields: today sums, 14-day daily series,
   top-3 models by cost, tool counts via JSON `data`, cost-per-day rows,
   totals.
4. Empty-DB edge: `load(from:)` returns nil (no today row).

**Verification:** test command green.
**Dependencies:** P3.

## P9 — CI + ROADMAP + final verification

**Goal:** the suite runs on every PR; milestone bookkeeping done; the OpenBox
extraction proven regression-free.

**Files touched:** `.github/workflows/deck.yml`, `ROADMAP.md`.

**Steps:**
1. deck.yml: after "Generate project", add a `Test` step running the test
   command (unsigned: `CODE_SIGNING_ALLOWED=NO` variant like the build step),
   with a zero-test guard — pipe output to a log, grep for
   `Test Suite 'All tests' (pass|fail)` and require "Executed ≥1 test", else
   exit 1.
2. ROADMAP.md: mark M4 "Tests" `[x]`; add deferred line under M4: DevBox
   parsers, RemoteOpenCodeLoader aggregation, ProcessSnapshot parsing, Loader
   formatters (untested) — worded so deck-next ranks them.
3. Final verification: full `xcodebuild -scheme DeckApp -configuration Release`
   build; re-add OpenBox from the gallery and confirm the front face (cost
   chart + model rows) renders identically.
4. Push branch; open PR with the standard summary (scope, refactors, CI step).

**Verification:** test command green; Release build green; OpenBox gallery
re-add clean.
**Dependencies:** all prior phases.

---

## Post-merge (handoff to deck-end-fast)

`def feat shared-parser-tests` after the PR merges.
