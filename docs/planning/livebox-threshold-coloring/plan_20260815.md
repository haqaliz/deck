# Plan: livebox-threshold-coloring

- Date: 2026-08-15
- Input: `docs/planning/livebox-threshold-coloring/prd.md` + `critique.md`
- Branch: `feat/livebox-threshold-coloring/aliz`

## Phase 1 — Shared threshold logic (TDD)

**Goal:** ship the reusable, testable threshold rules in Shared.

**Files touched:**
- `native/Shared/LiveBoxCore.swift` (new)
- `native/SharedTests/ThresholdTierTests.swift` (new)

**Steps:**
1. RED — write `ThresholdTierTests` first (pure logic, no code exists yet):
   - `tier(value:warn:alarm:)`: value < warn → `.normal`; value == warn → `.warn`;
     value == alarm → `.alarm`; value > alarm → `.alarm`; value between → `.warn`.
   - Precedence: warn = 90, alarm = 80 → value 85 → `.alarm`.
   - Boundary sanity: value 0 and value 100.
   - Color constants: `ThresholdTier.warnColor` / `alarmColor` RGB values are
     amber/red (assert red/green components so NetBox reuses the same language).
2. GREEN — implement `enum ThresholdTier` + static `tier(value:warn:alarm:)` +
   `warnColor`/`alarmColor` (`RGBA`), Foundation-only like `OpenBoxCore.swift`.
3. REFACTOR — none expected (tiny surface).

**Verification:** `xcodebuild test -project native/Deck.xcodeproj -scheme DeckSharedTests -derivedDataPath native/build-test` — new suite green.

**Dependencies:** none.

## Phase 2 — Settings keys + tolerant decode (TDD)

**Goal:** persist the three new settings without breaking old `settings.json`.

**Files touched:**
- `native/Shared/DeckSettings.swift` (`LiveBoxSettings`)
- `native/SharedTests/DecodeTests.swift` (`LiveBoxSettingsDecodeTests`)

**Steps:**
1. RED — extend `LiveBoxSettingsDecodeTests` (DecodeTests.swift:143):
   - `{}` decodes to defaults: `showThresholdColors == true`,
     `warnThreshold == 80`, `alarmThreshold == 90` (old-file compatibility).
   - Explicit values round-trip.
2. GREEN — add `showThresholdColors = true`, `warnThreshold = 80`,
   `alarmThreshold = 90` + `decodeIfPresent` entries in `init(from:)`.

**Verification:** DeckSharedTests green (Phase 1 suite still green too).

**Dependencies:** none (independent of Phase 1).

## Phase 3 — Front face coloring

**Goal:** rows and chart lines recolor by current-value tier.

**Files touched:**
- `native/DeckWidgets/LiveBoxWidget.swift` (`LiveBoxFace`)

**Steps:**
1. Add `private func tierColor(for value: Double) -> Color?` on `LiveBoxFace`:
   returns `nil` when `!settings.showThresholdColors` or tier is `.normal`;
   returns warn/alarm color from `ThresholdTier` otherwise.
2. `metricRow` (line 409): value text
   `.foregroundStyle(tierColor(for: value) ?? .primary)` — dot keeps the user
   color.
3. Chart (line 352): CPU/MEM/DISK `LineMark`s use
   `.foregroundStyle(tierColor(for: sample/latest value) ?? userColor)`; per-core
   lines each use `tierColor(for: coreValue) ?? cpuColor.opacity(0.4)` — each
   core by its own current value (interview decision).
4. UI is not unit-testable — keep the tier decision in Phase 1's helper; the
   view only maps tier → SwiftUI `Color`.

**Verification:** Release build; install `Deck.app`; in settings set Warn 5 /
Alarm 10 → rows + chart lines visibly amber/red on the next 5s tick; restore
80/90. Check all three sizes + light/dark material (brief caveat).

**Dependencies:** Phase 1 (helper) + Phase 2 (settings).

## Phase 4 — Back face settings

**Goal:** Thresholds section in the LiveBox settings tab.

**Files touched:**
- `native/DeckApp/DeckApp.swift` (`LiveBoxSettingsView`, line 303)

**Steps:**
1. New "Thresholds" `Section` after "Metrics":
   - `Toggle("Show threshold colors", isOn: $settings.showThresholdColors)`
   - `Stepper("Warn at: \(settings.warnThreshold)%", value: $settings.warnThreshold, in: 0...100)`
   - `Stepper("Alarm at: \(settings.alarmThreshold)%", value: $settings.alarmThreshold, in: 0...100)`
   - Caption: "Alarm takes precedence when it is lower than Warn." (critique amber #1)

**Verification:** Release build; install; toggle + steppers persist across app
relaunch; widget responds within a timeline regen.

**Dependencies:** Phase 2 (keys).

## Phase 5 — Docs + live verification

**Goal:** register the feature, close the roadmap item, verify on hardware.

**Files touched:**
- `ROADMAP.md` (line 69 → `[x]`; keep NetBox:81 open as the follow-on)
- `README.md` (LiveBox table row: mention threshold colors)

**Steps:**
1. Mark ROADMAP.md:69 shipped; add a mention to the README LiveBox row.
2. Live check: re-add from the gallery (regression rule), all sizes, light/dark
   widget materials, then leave the widget running with defaults for the amber
   sanity check (critique amber #2: MEM default behavior).

**Verification:** `git status` clean of stray artifacts; gallery re-add works.

**Dependencies:** Phases 1–4.

## Cross-phase rules

- Every phase ends with a green build (`xcodebuild ... build`) — never leave
  the worktree broken.
- Commit per phase on `feat/livebox-threshold-coloring/aliz` with the repo's
  conventional-commit style (`feat:` / `test:`), e.g. the earlier LiveBox work.
- Push + PR via `gh` when the last phase passes.
