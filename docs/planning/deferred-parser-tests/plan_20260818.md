# Plan: deferred-parser-tests

**Date:** 2026-08-18 · **Branch:** `feat/deferred-parser-tests/aliz`
**PRD:** `docs/planning/deferred-parser-tests/prd.md` (approved)
**Verification commands (repo conventions — no `swift build`/`swift run` here):**

```bash
xcodegen generate --spec native/project.yml
xcodebuild test -project native/Deck.xcodeproj -scheme DeckSharedTests \
  -derivedDataPath native/build            # pure logic
xcodebuild -project native/Deck.xcodeproj -scheme DeckApp -configuration Release \
  -derivedDataPath native/build build      # full shell
```

All phases are independent; order: zero-move slices first (fast green), then the
two extractions, then the ps behavior fix last (touches shipped behavior).
Every phase ends with the test suite green.

## Phase 1 — DevBox parser/formatter tests (no production changes)

**Goal:** pin `LsofParser` / `DockerParser` / `parsePercent` / `Formatters` behavior. Tests written first; they pass against shipped code as-is (behavior pins, per PRD §2).

**Files touched:** `native/SharedTests/DevBoxSnapshotTests.swift` (new).

**Steps:**
1. `LsofParserTests`: `p`/`c`/`n` field-mode block; dedup by `command|name`; empty host skipped; `n` before `c` skipped; port from last `:` (incl. IPv6 `[::1]:8080`); sort port asc, command asc.
2. `DockerParserTests`: ps+stats join by name; stats-only rows dropped; ps row missing from stats → nil percents; comma-name `a,b` → `a`; empty ps → `.noContainers`, non-empty → `.running`.
3. `DockerParserPercentTests`: `"0.05%"` → 0.05, `" 1.2% "` → 1.2, `"abc"`/`"5"`/`""` → nil.
4. `DevBoxFormattersTests`: `portLabel`; `percentString` — `"12.3%"`, nil → literal `"—"`.

**Verification:** `xcodebuild test -scheme DeckSharedTests` green.
**Dependencies:** none.

## Phase 2 — `PsParser` extraction + right-anchored fix (TDD, RED → GREEN)

**Goal:** extract pure `PsParser.parse` and fix paths-with-spaces (PRD §3.2, interview decision).

**Files touched:** `native/Shared/ProcessSnapshot.swift`, `native/SharedTests/ProcessSnapshotTests.swift` (new).

**Steps:**
1. RED: `PsParserTests` referencing `PsParser.parse(_ raw: String) -> [TopProcess]` — compile fails (no symbol).
2. Implement `PsParser` in `Shared/ProcessSnapshot.swift`: right-anchored — last two whitespace tokens are `%cpu`/`%mem`, remaining tokens joined with spaces as path → `lastPathComponent`; < 3 tokens skipped; unparseable cpu/mem → 0; sorted cpu desc.
3. GREEN: `HostProcessSampler.top` = `PsParser.parse(text)` + `prefix(limit)`.
4. Tests pin: `""` → []; header-like single line skipped; multi-space columns; `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome 1.5 2.0` → name `Google Chrome`; unparseable cpu → 0; sort desc by cpu; `prefix` behavior via sampler-shaped helper call.

**Verification:** suite green; `xcodebuild build -scheme DeckApp` compiles.
**Dependencies:** none (Phase 1 independent).

## Phase 3 — `BatteryCore` extraction to Shared (TDD)

**Goal:** move `BatteryFormatters` + `percent`/`timeMinutes` to `Shared/BatteryCore.swift` (LiveBoxDiskCore precedent — pure core only, no `import IOKit.ps` in test targets).

**Files touched:** `native/Shared/BatteryCore.swift` (new), `native/DeckWidgets/Loaders/BatteryMetrics.swift` (edit), `native/SharedTests/BatteryCoreTests.swift` (new).

**Steps:**
1. RED: `BatteryCoreTests` referencing `BatteryFormatters` + `BatteryMath` — compile fails.
2. Implement `Shared/BatteryCore.swift`: `BatteryFormatters` moved verbatim (`formatPercent` "71%", `formatTime` nil → literal `"—"`, "45m", "6h 35m"; `formatState` precedence AC Power → Full → Charging → Discharging) + `BatteryMath.percent(current:max:)` (clamp 0...100, max ≤ 0 → nil) + `BatteryMath.timeMinutes(seconds:)` (≤ 0 → nil, rounded).
3. GREEN: edit `BatteryMetrics.swift` — delete private copies of the formatters and the two helpers, call the Shared ones; keep `BatterySnapshot`/`PowerSource`/loader in place.

**Verification:** suite green; DeckApp Release build; BatBox re-added from gallery renders as before.
**Dependencies:** none.

## Phase 4 — `RemoteOpenCodeAggregator` extraction (TDD)

**Goal:** make the two `aggregate` overloads + helpers internal and testable; `load()` wire byte-identical (PRD §3.1 — one type set, no fork).

**Files touched:** `native/Shared/RemoteOpenCodeLoader.swift` (edit), `native/SharedTests/RemoteOpenCodeAggregatorTests.swift` (new).

**Steps:**
1. RED: aggregator tests referencing `RemoteOpenCodeAggregator` + fixture `RemoteSession`/`RemoteMessage` shapes — compile fails.
2. Refactor `RemoteOpenCodeLoader.swift`: `enum RemoteOpenCodeAggregator` holding the fixture types (Codable, same shapes `load()` decodes), both `aggregate` overloads, `utcDayString`, `round4`, `modelKey`, window constants; `load()` decodes into these types and calls the aggregator.
3. GREEN, session-mode suite: 14d window on `time.updated`; totalInput/output/cost; today's sessions count + input/output/cost; UTC day bucketing; `modelKey` (`provider/id`, `local/unknown`); top-3 models cost desc; `costDaily` day asc / cost desc within day; `round4`.
4. Message-mode suite: `role == "assistant"` only; 13d message window; inactive-session messages dropped; `countedSessionIDs` (sessions) vs `todaySessionIDs` (today's messages); today totals; modelKey `provider/model` + `local/unknown`.
5. `utcDayStringTests`: epoch ms just before/after UTC midnight.

**Verification:** suite green; DeckApp build; no dead code (private originals deleted).
**Dependencies:** none.

## Phase 5 — ROADMAP update + full verification

**Goal:** record the covered items; full green run.

**Files touched:** `ROADMAP.md`.

**Steps:**
1. Update the M4 deferred line: DevBox parsers, RemoteOpenCodeLoader aggregation, ProcessSnapshot parsing, BatteryMetrics formatters → covered; SystemMetrics per-core math remains deferred (needs LiveBox widget file touch).
2. Full pass: `xcodegen generate` → test suite green → DeckApp Release build.
3. Gallery sanity: re-add DevBox, LiveBox, BatBox; verify all three sizes render as before (no visual regression from the refactors).

**Verification:** suite green, Release build succeeds, gallery re-add clean.
**Dependencies:** Phases 1-4.

## Risks

- **ps output shape drift** (macOS `ps -Aceo comm=,...` column layout): pinned by tests in Phase 2; the right-anchored parse is robust to comm-with-spaces either way (`-c` basename or full path).
- **`String(format:)` locale**: existing formatters already use it and are tested on this machine; no locale handling added (out of scope, consistent with `formatRate`).
